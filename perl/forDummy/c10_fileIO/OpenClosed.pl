#!/usr/bin/perl -w

# A program to open and close files
#
# open(HANDLE, FILENAME): handle可以是任何的大写的单词
# 例如：
# 	1. open(TEST, "<test.txt"): 只读打开
# 	2. open(TEST, ">test.txt"): 可写打开
# 	3. open(TEST, ">>test.txt"):可添加打开
# close(HANDLE)
